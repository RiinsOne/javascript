div блочный элемент
span строчный

скрипты подключаются в конце перед </body>
чтобы html отрисовался, а потом подгружались скрипты
т.е. все теги проходятся и создаются и потом грузятся скрипты

в js нельзя умножить строку на число, будет NaN

undefined (let a) не присваивается, а null присвают (let a = null).

#
function calc(arr) {
 let sum = 0
 for (let i = 0; i < arr.length; i++) {
  if (arr[i] > 0 && arr[i] % 2 !==0) {
   calculate += arr[i];
   }
  }
 return sum;
}




