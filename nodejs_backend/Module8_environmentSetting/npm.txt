Посмотреть, в какую директорию npm складывает ссылки
на исполняемые файлы можно с помощью
команды npm bin -g или npm bin --global


echo $PATH
echo $PATH | grep /usr/bin
посмотреть содержание переменной окружения PATH



//
